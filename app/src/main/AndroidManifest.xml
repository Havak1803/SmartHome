<?xml version="1.0" encoding="utf-8"?>
<!--
    Smart Home App v3.1 - AndroidManifest.xml
    HYBRID ARCHITECTURE: MQTT + Firebase
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================================================ -->
    <!-- REQUIRED PERMISSIONS -->
    <!-- ================================================ -->

    <!-- Internet access for MQTT and Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Check network state (connection monitoring) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Keep device awake during MQTT operations -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Optional: For WiFi state monitoring -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Push notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHome"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Login Activity (Entry Point) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartHome" />

        <!-- ================================================ -->
        <!-- MQTT SERVICE (Paho Android Service) -->
        <!-- Required for background MQTT operations -->
        <!-- ================================================ -->
        <service android:name="org.eclipse.paho.android.service.MqttService" />

    </application>

</manifest>
